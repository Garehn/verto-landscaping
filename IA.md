# Verto Landscapes — Homepage IA (v2)

Single scrolling cinematic page. Other routes keep their jobs but inherit the
system. Section order and jobs:

0. **Preloader** — VERTO wordmark + 0–100 counter, curtain lift. Once/session.
1. **Hero (100svh)** — full-bleed garden image, slow scale-in, dark grade.
   Corner meta labels. Headline "Gardens that hold time." rises per-word.
   Job: instant calibre signal.
2. **Manifesto (scrub)** — intro paragraph as large display text, words light
   up from 15% → 100% opacity as you scroll through a tall sticky section.
   Job: voice + slow-on-purpose positioning.
3. **Selected work (cinematic panels)** — 3 full-viewport sticky-stacked
   projects, parallax image, mono index + location, title reveal.
   Job: proof. CTA → portfolio.
4. **Stats band** — 3 counters (years, projects/yr, care visits) on ink.
5. **Services (editorial index)** — numbered rows 01–06, hover reveals a
   floating image preview; row border lights up. Job: scope of capability.
6. **Terrain (3D moment)** — R3F topographic contour terrain, undulating,
   scroll-linked camera; statement text overlaid. Job: the "whoa" moment,
   on-brand (shaping land).
7. **Before/After** — keep existing slider, refit to dark frame + mono labels.
8. **Testimonial** — huge serif quote, word-stagger reveal, brass rule.
9. **CTA** — "Let's begin." huge display + contact card (Rory, phone, email,
   address). Marquee strip above.
10. **Footer** — fat editorial: nav, contact, ABN placeholder, coordinates,
    giant VERTO wordmark cropped at bottom.

Copy: reuse existing lib/content.ts voice verbatim where possible; new micro
labels only. Contact details = Rory / 164 Edinburgh Rd Castlecrag NSW 2068 /
0488 728 767 / rory@vertolandscapes.com everywhere (footer, CTA, contact page,
JSON-LD).
